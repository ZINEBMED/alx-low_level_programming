#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (letter = a; a++)
		putchar("the caracter enter should be written in lowercase");
}
return (0);
