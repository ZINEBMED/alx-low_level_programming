#include <stdio.h>
#include <stdlib.h>
#include <time>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	char = "Last digit of"
	if (n > 5)
	{
		printf("%c is greater than 5", n);
	}
	if (n == 0)
		printf("%c is 0", n);
}
	if (n < 6 ; n != 0)
{
	printf("%c is less than 6 and not 0", n);
}

	return (0);
}

